ПРОМПТ ДЛЯ ПОСЛЕДОВАТЕЛЬНОГО ИЗУЧЕНИЯ И РЕАЛИЗАЦИИ PONG
==========================================================

КАК ИСПОЛЬЗОВАТЬ ЭТОТ ФАЙЛ

Полностью скопируй содержимое этого TXT-файла в новый AI-чат.
AI должен изучить требования и два готовых исходника, а затем провести тебя по реализации в том же
порядке, что и исходное задание: сначала обязательная пошаговая версия, потом бонусная интерактивная.


ПРОМПТ ДЛЯ AI-АГЕНТА
====================

Ты — преподаватель языка C и наставник студента первого курса. В этом файле после промпта находятся:

1. Условия основной части задания.
2. Условия бонусной части задания.
3. Готовый код основной пошаговой версии pong.c.
4. Готовый код бонусной интерактивной версии pong_interactive.c.

Твоя задача — помочь мне понять, как последовательно написать и реализовать обе версии. Не выдавай всё
объяснение одним большим сообщением. Обучение должно идти строго в том порядке, который задан в README:
сначала основная часть, затем бонус.

ОБЩИЕ ПРАВИЛА ОБУЧЕНИЯ

1. Общайся на русском языке.
2. Считай, что я студент первого курса и знаю только переменные, условия, циклы и простые функции.
3. Любой новый термин сразу объясняй простыми словами.
4. Используй готовые исходники в конце файла как эталон реализации.
5. Не придумывай функции или возможности, которых нет в приложенном коде.
6. Не усложняй решение массивами, структурами, пользовательскими указателями или дополнительными
   файлами.
7. Всегда разделяй:
   - обязательное требование README;
   - техническое решение, необходимое для реализации;
   - необязательное оформление или косметику.
8. Не переходи к бонусной части, пока я явно не напишу, что готов перейти к бонусу.
9. После каждого крупного этапа задавай 2–3 коротких вопроса для проверки понимания.
10. Если я отвечу неправильно, сначала дай подсказку, а затем объясни ответ.
11. Когда показываешь полный итоговый код, используй код из этого файла без незаявленных изменений.
12. Объясняй не только что делает строка, но и зачем она нужна в игровой логике.

ЭТАП 1. ОСНОВНАЯ ПОШАГОВАЯ ВЕРСИЯ

Начни именно с этого этапа:

1. Кратко перескажи условия основной части.
2. Составь простой план программы без кода.
3. Объясни, какие значения нужно хранить:
   - позиции двух ракеток;
   - координаты мяча;
   - направления мяча;
   - счёт игроков;
   - введённую команду.
4. Объясни назначение констант #define.
5. Составь псевдокод одного игрового хода.
6. Последовательно разбери функции основной версии:
   - is_action;
   - read_action;
   - move_paddle;
   - vertical_direction;
   - horizontal_direction;
   - field_symbol;
   - draw_field;
   - print_winner;
   - main.
7. Отдельно и с числовыми примерами объясни:
   - ограничения движения ракеток;
   - отскок от верхней и нижней границы;
   - попадание мяча в ракетку;
   - гол;
   - увеличение счёта;
   - возврат мяча в центр.
8. Покажи, как программа реагирует на A, Z, K, M, пробел и неправильный символ.
9. Разбери готовый pong.c по логическим блокам.
10. Покажи команды компиляции, запуска, clang-format и cppcheck.
11. Подготовь короткий рассказ, с которым можно защитить основную часть.
12. Проведи мини-проверку знаний.
13. После завершения остановись и спроси дословно:
    «Основная версия разобрана. Перейти к бонусной интерактивной версии?»

До моего положительного ответа не объясняй ncurses и pong_interactive.c.

ЭТАП 2. БОНУСНАЯ ИНТЕРАКТИВНАЯ ВЕРСИЯ

Переходи к этому этапу только после моего согласия:

1. Напомни, что бонус выполняется после основной части.
2. Объясни, какие части логики переносятся из pong.c без изменения.
3. Объясни главное отличие: цикл работает постоянно, даже если клавиша не нажата.
4. Простыми словами объясни функции ncurses:
   - initscr;
   - noecho;
   - curs_set;
   - timeout;
   - getch;
   - erase;
   - mvprintw;
   - mvaddch;
   - refresh;
   - napms;
   - endwin.
5. Объясни FRAME_DELAY и один кадр интерактивной игры.
6. Разбери pong_interactive.c по логическим блокам.
7. Сравни обе версии в таблице: ввод, движение мяча, отрисовка, задержка и завершение.
8. Покажи команду сборки с -lncurses и запуск.
9. Подготовь рассказ для защиты бонусной части.
10. Проведи итоговую проверку знаний по обеим версиям.
11. В конце попроси меня самостоятельно объяснить всю программу и исправь неточности.

Начни ответ только с ЭТАПА 1. Не переходи сразу к разбору кода бонуса.


УСЛОВИЯ ОСНОВНОГО ЗАДАНИЯ
=========================

Нужно разработать src/pong.c — терминальную игру Pong для двух игроков.

Обязательные требования:

- язык C;
- пошаговый режим;
- только ASCII-графика;
- прямоугольное поле 80×25 символов;
- две ракетки размером по 3 символа;
- мяч размером 1 символ;
- A/Z двигают левую ракетку;
- K/M двигают правую ракетку;
- пробел пропускает ход;
- после правильной команды выполняется игровой шаг и поле рисуется заново;
- после неправильной команды программа продолжает ждать правильный ввод;
- игра продолжается до 21 очка;
- после победы выводится поздравление и программа завершается;
- основная часть использует только стандартную библиотеку C;
- нельзя использовать system() и аналогичные системные вызовы;
- нельзя использовать динамическую память;
- нельзя использовать массивы;
- нельзя использовать пользовательские указатели;
- код должен соответствовать структурному программированию;
- код проверяется clang-format и cppcheck.


УСЛОВИЯ БОНУСНОГО ЗАДАНИЯ
========================

После готовой основной части нужно разработать src/pong_interactive.c.

Требования бонуса:

- игра работает в реальном времени;
- мяч движется без ожидания Enter;
- клавиши обрабатываются во время игры;
- разрешена библиотека ncurses;
- ограничения основной части продолжают действовать;
- бонус выполняется только после основной части.


ГОТОВЫЙ КОД ОСНОВНОЙ ВЕРСИИ: pong.c
====================================

#include <stdio.h>

#define FIELD_WIDTH 80
#define FIELD_HEIGHT 25
#define LEFT_PADDLE_X 2
#define RIGHT_PADDLE_X 77
#define PADDLE_HEIGHT 3
#define START_PADDLE_Y 11
#define START_BALL_X 40
#define START_BALL_Y 12
#define WIN_SCORE 21

static int is_action(int action);
static int read_action(void);
static int move_paddle(int paddle_y, int action, int up_key, int down_key);
static int vertical_direction(int ball_y, int direction_y);
static int horizontal_direction(int ball_x, int ball_y, int direction_x, int left_y, int right_y);
static char field_symbol(int x, int y, int ball_x, int ball_y, int left_y, int right_y);
static void draw_field(int ball_x, int ball_y, int left_y, int right_y, int left_score, int right_score);
static void print_winner(int left_score);

int main(void) {
    int left_y = START_PADDLE_Y;
    int right_y = START_PADDLE_Y;
    int ball_x = START_BALL_X;
    int ball_y = START_BALL_Y;
    int direction_x = 1;
    int direction_y = 1;
    int left_score = 0;
    int right_score = 0;
    int action = 0;

    draw_field(ball_x, ball_y, left_y, right_y, left_score, right_score);
    while (left_score < WIN_SCORE && right_score < WIN_SCORE && action != EOF) {
        action = read_action();
        if (action != EOF) {
            left_y = move_paddle(left_y, action, 'a', 'z');
            right_y = move_paddle(right_y, action, 'k', 'm');
            direction_y = vertical_direction(ball_y, direction_y);
            ball_y += direction_y;
            direction_x = horizontal_direction(ball_x, ball_y, direction_x, left_y, right_y);
            ball_x += direction_x;
            if (ball_x == 0) {
                right_score++;
            } else if (ball_x == FIELD_WIDTH - 1) {
                left_score++;
            }
            if (ball_x == 0 || ball_x == FIELD_WIDTH - 1) {
                ball_x = START_BALL_X;
                ball_y = START_BALL_Y;
                direction_x = (left_score + right_score) % 2 == 0 ? 1 : -1;
                direction_y = (left_score + right_score) % 2 == 0 ? -1 : 1;
            }
            draw_field(ball_x, ball_y, left_y, right_y, left_score, right_score);
        }
    }
    if (left_score == WIN_SCORE || right_score == WIN_SCORE) {
        print_winner(left_score);
    }
    return 0;
}

static int is_action(int action) {
    int result = 0;
    if (action == 'a' || action == 'A' || action == 'z' || action == 'Z' || action == 'k' || action == 'K' ||
        action == 'm' || action == 'M' || action == ' ') {
        result = 1;
    }
    return result;
}

static int read_action(void) {
    int action = 0;
    int valid = 0;
    while (valid == 0 && action != EOF) {
        action = getchar();
        valid = is_action(action);
    }
    return action;
}

static int move_paddle(int paddle_y, int action, int up_key, int down_key) {
    if ((action == up_key || action == up_key - 'a' + 'A') && paddle_y > 1) {
        paddle_y--;
    } else if ((action == down_key || action == down_key - 'a' + 'A') &&
               paddle_y + PADDLE_HEIGHT < FIELD_HEIGHT - 1) {
        paddle_y++;
    }
    return paddle_y;
}

static int vertical_direction(int ball_y, int direction_y) {
    if (ball_y + direction_y == 0 || ball_y + direction_y == FIELD_HEIGHT - 1) {
        direction_y = -direction_y;
    }
    return direction_y;
}

static int horizontal_direction(int ball_x, int ball_y, int direction_x, int left_y, int right_y) {
    int next_x = ball_x + direction_x;
    int hits_left =
        direction_x < 0 && next_x == LEFT_PADDLE_X && ball_y >= left_y && ball_y < left_y + PADDLE_HEIGHT;
    int hits_right =
        direction_x > 0 && next_x == RIGHT_PADDLE_X && ball_y >= right_y && ball_y < right_y + PADDLE_HEIGHT;
    if (hits_left || hits_right) {
        direction_x = -direction_x;
    }
    return direction_x;
}

static char field_symbol(int x, int y, int ball_x, int ball_y, int left_y, int right_y) {
    char symbol = ' ';
    if (y == 0 || y == FIELD_HEIGHT - 1) {
        symbol = '-';
    } else if (x == 0 || x == FIELD_WIDTH - 1) {
        symbol = '|';
    } else if (x == ball_x && y == ball_y) {
        symbol = 'o';
    } else if ((x == LEFT_PADDLE_X && y >= left_y && y < left_y + PADDLE_HEIGHT) ||
               (x == RIGHT_PADDLE_X && y >= right_y && y < right_y + PADDLE_HEIGHT)) {
        symbol = '|';
    } else if (x == FIELD_WIDTH / 2 && y % 2 == 0) {
        symbol = ':';
    }
    return symbol;
}

static void draw_field(int ball_x, int ball_y, int left_y, int right_y, int left_score, int right_score) {
    printf("\nScore: %d - %d\n", left_score, right_score);
    for (int y = 0; y < FIELD_HEIGHT; y++) {
        for (int x = 0; x < FIELD_WIDTH; x++) {
            printf("%c", field_symbol(x, y, ball_x, ball_y, left_y, right_y));
        }
        printf("\n");
    }
}

static void print_winner(int left_score) {
    if (left_score == WIN_SCORE) {
        printf("Congratulations! The left player wins!\n");
    } else {
        printf("Congratulations! The right player wins!\n");
    }
}


ГОТОВЫЙ КОД БОНУСНОЙ ВЕРСИИ: pong_interactive.c
================================================

#include <ncurses.h>

#define FIELD_WIDTH 80
#define FIELD_HEIGHT 25
#define LEFT_PADDLE_X 2
#define RIGHT_PADDLE_X 77
#define PADDLE_HEIGHT 3
#define START_PADDLE_Y 11
#define START_BALL_X 40
#define START_BALL_Y 12
#define WIN_SCORE 21
#define FRAME_DELAY 70

static int move_paddle(int paddle_y, int action, int up_key, int down_key);
static int vertical_direction(int ball_y, int direction_y);
static int horizontal_direction(int ball_x, int ball_y, int direction_x, int left_y, int right_y);
static char field_symbol(int x, int y, int ball_x, int ball_y, int left_y, int right_y);
static void draw_field(int ball_x, int ball_y, int left_y, int right_y, int left_score, int right_score);
static void show_winner(int left_score);

int main(void) {
    int left_y = START_PADDLE_Y;
    int right_y = START_PADDLE_Y;
    int ball_x = START_BALL_X;
    int ball_y = START_BALL_Y;
    int direction_x = 1;
    int direction_y = 1;
    int left_score = 0;
    int right_score = 0;

    initscr();
    noecho();
    curs_set(0);
    timeout(0);
    draw_field(ball_x, ball_y, left_y, right_y, left_score, right_score);
    while (left_score < WIN_SCORE && right_score < WIN_SCORE) {
        int action = getch();
        left_y = move_paddle(left_y, action, 'a', 'z');
        right_y = move_paddle(right_y, action, 'k', 'm');
        direction_y = vertical_direction(ball_y, direction_y);
        ball_y += direction_y;
        direction_x = horizontal_direction(ball_x, ball_y, direction_x, left_y, right_y);
        ball_x += direction_x;
        if (ball_x == 0) {
            right_score++;
        } else if (ball_x == FIELD_WIDTH - 1) {
            left_score++;
        }
        if (ball_x == 0 || ball_x == FIELD_WIDTH - 1) {
            ball_x = START_BALL_X;
            ball_y = START_BALL_Y;
            direction_x = (left_score + right_score) % 2 == 0 ? 1 : -1;
            direction_y = (left_score + right_score) % 2 == 0 ? -1 : 1;
        }
        draw_field(ball_x, ball_y, left_y, right_y, left_score, right_score);
        napms(FRAME_DELAY);
    }
    show_winner(left_score);
    endwin();
    return 0;
}

static int move_paddle(int paddle_y, int action, int up_key, int down_key) {
    if ((action == up_key || action == up_key - 'a' + 'A') && paddle_y > 1) {
        paddle_y--;
    } else if ((action == down_key || action == down_key - 'a' + 'A') &&
               paddle_y + PADDLE_HEIGHT < FIELD_HEIGHT - 1) {
        paddle_y++;
    }
    return paddle_y;
}

static int vertical_direction(int ball_y, int direction_y) {
    if (ball_y + direction_y == 0 || ball_y + direction_y == FIELD_HEIGHT - 1) {
        direction_y = -direction_y;
    }
    return direction_y;
}

static int horizontal_direction(int ball_x, int ball_y, int direction_x, int left_y, int right_y) {
    int next_x = ball_x + direction_x;
    int hits_left =
        direction_x < 0 && next_x == LEFT_PADDLE_X && ball_y >= left_y && ball_y < left_y + PADDLE_HEIGHT;
    int hits_right =
        direction_x > 0 && next_x == RIGHT_PADDLE_X && ball_y >= right_y && ball_y < right_y + PADDLE_HEIGHT;
    if (hits_left || hits_right) {
        direction_x = -direction_x;
    }
    return direction_x;
}

static char field_symbol(int x, int y, int ball_x, int ball_y, int left_y, int right_y) {
    char symbol = ' ';
    if (y == 0 || y == FIELD_HEIGHT - 1) {
        symbol = '-';
    } else if (x == 0 || x == FIELD_WIDTH - 1) {
        symbol = '|';
    } else if (x == ball_x && y == ball_y) {
        symbol = 'o';
    } else if ((x == LEFT_PADDLE_X && y >= left_y && y < left_y + PADDLE_HEIGHT) ||
               (x == RIGHT_PADDLE_X && y >= right_y && y < right_y + PADDLE_HEIGHT)) {
        symbol = '|';
    } else if (x == FIELD_WIDTH / 2 && y % 2 == 0) {
        symbol = ':';
    }
    return symbol;
}

static void draw_field(int ball_x, int ball_y, int left_y, int right_y, int left_score, int right_score) {
    erase();
    mvprintw(0, 0, "Score: %d - %d", left_score, right_score);
    for (int y = 0; y < FIELD_HEIGHT; y++) {
        for (int x = 0; x < FIELD_WIDTH; x++) {
            mvaddch(y + 1, x, field_symbol(x, y, ball_x, ball_y, left_y, right_y));
        }
    }
    refresh();
}

static void show_winner(int left_score) {
    erase();
    if (left_score == WIN_SCORE) {
        mvprintw(0, 0, "Congratulations! The left player wins!");
    } else {
        mvprintw(0, 0, "Congratulations! The right player wins!");
    }
    refresh();
    napms(2000);
}


КОМАНДЫ ДЛЯ САМОСТОЯТЕЛЬНОЙ ПРОВЕРКИ
====================================

Сборка основной версии из корня проекта:

cc -std=c11 -Wall -Wextra -Werror src/pong.c -o pong

Запуск:

./pong

Сборка бонусной версии:

cc -std=c11 -Wall -Wextra -Werror src/pong_interactive.c -lncurses -o pong_interactive

Запуск:

./pong_interactive

Проверка форматирования:

clang-format -n --Werror --style=file:materials/linters/.clang-format src/pong.c
clang-format -n --Werror --style=file:materials/linters/.clang-format src/pong_interactive.c

Статический анализ:

cppcheck --enable=all --suppress=missingIncludeSystem src/pong.c
cppcheck --enable=all --suppress=missingIncludeSystem src/pong_interactive.c

